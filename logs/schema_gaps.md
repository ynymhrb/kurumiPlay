# Schema Gaps

`spec/CEU_schema.md` 不是定案。每条 CEU 的 `schema_gap` 字段（如果填了）汇总到这里，作为"当前 schema 哪里不够用"的原始信号池，供卷末的 schema 复核步骤（见 `spec/eval_protocol.md` 第7节）处理。

不允许无声丢弃：每次复核后，无论是否触发 schema 修改，都要在下方"复核记录"里写一条结论（改了/没改+原因），不能只处理信号池里的条目却不留痕迹。

## 待复核信号

（当前无待复核信号——C-001/E-002/E-004/F-001已于2026-07-18通过引入psych_core/literary_technique字段
和attribution_framework.md归因方法论处理完，见下方复核记录。后续卷新出现的信号会继续累积到这里）

## 复核记录

| 日期 | 复核范围 | 结论 | 是否触发schema修改 | 对应revision_log条目 |
|---|---|---|---|---|
| 2026-07-18 | 卷一末尾复核，3条待复核信号（A-001/C-002/S-002） | A-001（起源类证据分类）：暂不需要新分类，只有1个场景，`evidence_source: narrator`勉强够用，revisit if 2+更多同类案例出现。C-002（无冲突的记录方式）：**触发修改**，value_conflict新增"已核实无冲突"的显式写法，不是新字段，是用法规范。S-002（私下战略人格vs情绪失控的区分）：暂不需要CEU schema层面改动，现有emotion/underlying_belief字段配合MM8候选已经能记录这个区别，只有1个场景，不需要为此新增结构化字段；已在CEU_schema.md"待验证的开放问题"里补充这条证据的交叉引用 | 是（C-002一条） | CEU_schema.md v0.5→v0.6 |
| 2026-07-18 | 卷三累积4条信号（C-001/E-002/E-004/F-001），用户主动发起深度讨论，引入外部心理学框架（双重加工理论/依恋理论/IFS/认知失调等）共同设计归因方法论 | **触发重大修改**：4条信号的共同点是choice/value_conflict字段预设"当事人清楚自己在权衡什么"这个前提不总成立（反射反应/认知自我说服/表态vs情绪分层/依恋创伤触发）。讨论后发现更根本的问题——这些证据的原文具体表现形式混杂了"可迁移的心理真实"和"只在该场景有效的文学化表现手法"两种不同性质的信息，现有schema无法区分。解决方案：新增`spec/attribution_framework.md`（双轴归因分析法：先假设为真人走心理轴四层，只有心理轴出现"解释赤字"才调用文学轴两层，产出"心理完全闭环"或"心理燃料+文学外壳"判定），新增CEU schema字段`psych_core`（去场景化的可迁移反应模板）+`literary_technique`（可复用的叙事技法标签，供生成阶段按场景概率性套用，不是提取阶段丢弃）。4条信号分别用新框架分析并回填：C-001判定"心理燃料+文学外壳"（首次实例记入`reaction_stylization.md`），E-002/E-004/F-001判定"心理完全闭环" | 是（4条全部处理，新增2个字段+1个新spec文件+1个新Character OS文件类型） | CEU_schema.md v0.6→v0.7 |
